[project]
name = "phantom"
version = "0.1.0"
description = "Claude Code · DeepAgent · CopilotKit 에이전트 구축 데모"
requires-python = ">=3.11"
dependencies = [
    "langchain-google-genai",
    "python-dotenv",
]
