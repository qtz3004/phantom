[project]
name = "phantom"
version = "0.1.0"
description = "DeepAgent 기반 에이전트 프로젝트"
requires-python = ">=3.11"
dependencies = [
    "deepagents",
    "langchain-google-genai",
    "tavily-python",
    "python-dotenv",
    "copilotkit",
    "fastapi",
    "uvicorn[standard]",
]
